package com.vailter.standard;

import org.junit.jupiter.api.Test;

import java.util.Arrays;
import java.util.concurrent.TimeUnit;

public class PrettyCode {
    /**
     * 洗牌算法
     * 对于给定的 n 个元素，生成的那个排列，每一个元素都能等概率地出现在每一个位置。
     * 换句话说，每一个位置都能等概率地放置每个元素。
     * <p>
     * 代码如下（JDK中Collections.shuffle()也是这样实现的）：
     */
    @Test
    void testSort() {
        //String[] arr = {"1", "2", "3", "4", "5", "6",};
        //int n = 6;
        //for (int i = n - 1; i >= 0; i--) {
        //    // rand(0, i) 生成 [0, i] 之间的随机整数
        //    swap(arr[i], arr[rand(0, i)]);
        //}
        //long i = 0x5f3759df - (i >> 1);
        System.out.println(0x5f375a86);
        System.out.println(0x5f37642f);
    }
}